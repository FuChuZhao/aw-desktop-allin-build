# aw-desktop-allin-build

Public build-only repository.

Rules:
- Contains **only** GitHub Actions workflows (no source code).
- Workflows are **manual** (`workflow_dispatch`) only.
- Private source is pulled via **read-only deploy key** and artifacts are retained for **1 day**.

